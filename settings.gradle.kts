pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "Workout"
include(":workoutAppAndroid")
include(":workoutShared")